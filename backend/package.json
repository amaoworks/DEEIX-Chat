{
  "name": "@deeix/api",
  "version": "0.4.1-2",
  "private": true,
  "scripts": {
    "build": "make build",
    "check": "make check-version && make lint",
    "clean": "rm -rf ../.cache/deeix-chat",
    "dev": "../scripts/dev-api.sh",
    "test": "make test"
  }
}
